//  BFCacheJS.swift
//  Step 1~4 JavaScript 생성 코드 제거됨
//  복원은 WKWebView.interactionState 기반으로 이관

import UIKit
import WebKit
import SwiftUI

// Step 1~4 스크립트 생성 함수 전체 제거
// webView.goBack() / webView.goForward() 가 스크롤 상태를 자동 복원
extension BFCacheSnapshot {}
